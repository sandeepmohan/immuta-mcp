apiVersion: v1
kind: Service
metadata:
  name: {{ include "immuta-mcp.fullname" . }}
  labels:
    {{- include "immuta-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "immuta-mcp.selectorLabels" . | nindent 4 }}
